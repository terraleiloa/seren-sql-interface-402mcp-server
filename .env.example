# x402 MCP Server Configuration

# Gateway endpoint - handles payment verification and on-chain settlement
X402_GATEWAY_URL=https://x402.serendb.com

# Wallet configuration - how users sign EIP-712 payment authorizations
# Options: browser | walletconnect | hardware
WALLET_TYPE=browser

# WalletConnect (required if WALLET_TYPE=walletconnect)
# Get from https://cloud.walletconnect.com/
WALLETCONNECT_PROJECT_ID=

# Base RPC URL - used for balance checks only
# Settlement is handled by the x402 gateway
BASE_RPC_URL=https://mainnet.base.org

# Development
NODE_ENV=development
LOG_LEVEL=debug

# Future: Paloma cross-chain relay integration
# Paloma would allow users to relay transactions without their own RPC
# Requires: CosmWasm contract deployment, Compass-EVM setup, paloma.js
# See: https://github.com/palomachain/paloma.js
# PALOMA_LCD_URL=https://lcd.palomachain.com
# PALOMA_CHAIN_ID=tumbler
    